```
#创建项目
npm create vite@latest my-app -- --template react

react 
javascript+swc

概念
组件：返回标签的JavaScript函数 （有自己外(html，css)和内（javascript））

组件必须返回单个jsx元素 `<div>...</div>` 或使用空的 `<>...</>`

jsx标签：的组件也不能返回多个 JSX 标签 

添加样式className

{}返回到javascript



通常使用 `onSomething` 命名代表事件的 props，使用 `handleSomething` 命名处理这些事件的函数。
```